# Alena — внутренняя SPA (по блокам)

## Как запустить

Важно: не открывайте `index.html` через `file://` — браузер может блокировать module‑импорты.

1) Откройте PowerShell в папке проекта `Alena`

2) Запустите локальный сервер:

```powershell
py -m http.server 5173
```

3) Откройте в браузере:

`http://localhost:5173/`

## Настройка Supabase (для Блока 01)

В файле `app.js` заполните:

- `SUPABASE_URL` (Project URL)
- `SUPABASE_ANON_KEY` (anon public key)

Также нужна таблица `employees` с полями:
- `auth_user_id`
- `is_active`

При входе приложение проверяет, что:
- запись в `employees` существует
- `is_active = true`

## Интеграция с Telegram (Блок 07)

Приложение поддерживает привязку Telegram аккаунта и автоматические уведомления.

### Быстрый старт:

1. Выполните SQL из `TELEGRAM_SETUP.md` (создание таблицы `telegram_link_tokens`)
2. Деплойте Edge Functions через Supabase Dashboard (см. `TELEGRAM_SETUP.md`)
3. Настройте webhook для привязки аккаунтов
4. Настройте Cron job для ежедневных уведомлений

### Функции:

**1. Привязка аккаунта**
- В приложении: кнопка "📱 Подключить Telegram" (красная, если не привязан)
- Перейдите по ссылке в бота `@money_cheking_bot`
- Бот автоматически привяжет ваш аккаунт
- После привязки кнопка становится синей: "📱 Telegram подключён"

**2. Автоматические уведомления**
- Каждый день с 19-го числа в 12:00 МСК
- Напоминание о внесении показаний за текущий месяц
- Отправляется только если есть объекты без данных
- Список объектов, по которым нет показаний

**Подробные инструкции:** 
- Привязка: `TELEGRAM_SETUP.md`
- Уведомления: `TELEGRAM_NOTIFICATIONS_SETUP.md`

## Функционал системы

- ✅ Авторизация (Supabase Auth)
- ✅ Главное меню с плитками
- ✅ Внести показания счётчиков
- ✅ Создать объект учёта
- ✅ Редактировать объект и счётчики
- ✅ Архив объектов
- ✅ Статистика по объектам
- ✅ Привязка Telegram для уведомлений

