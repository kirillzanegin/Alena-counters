# 🔄 Передача задачи следующему агенту

## ✅ ЧТО УЖЕ СДЕЛАНО:

### 1. Telegram интеграция (способ 1 - старый) - РАБОТАЕТ ✅
- Привязка через веб-приложение → генерация токена → переход в бота
- Edge Function `telegram-webhook` задеплоена и работает
- Webhook настроен: `https://api.telegram.org/bot8477674658:AAHdZS8bGIKINlXawLoNJiuukywWQgAt3E0/setWebhook`
- **ВАЖНО:** В настройках функции отключено "Verify JWT" (иначе 401 ошибка)

### 2. UI кнопка Telegram - ГОТОВО ✅
- Кнопка в главном меню (под именем сотрудника):
  - 🔴 Красная с белой точкой = не привязан
  - 🔵 Синий контур (прозрачный фон) = привязан
- Размер: компактная (height: 28px)

### 3. База данных - ОБНОВЛЕНА ✅
- Таблица `telegram_link_tokens` имеет поля:
  - `employee_id` (BIGINT, nullable)
  - `tg_id` (TEXT, nullable)
  - `token` (TEXT)
  - `expires_at` (TIMESTAMP)
  - `used` (BOOLEAN)
- SQL из файла `SQL_UPDATE_TELEGRAM_TOKENS.sql` **ВЫПОЛНЕН**
- RLS политики обновлены для поддержки обоих способов

### 4. Код для нового способа - НАПИСАН ✅
- В `app.js` добавлен компонент `TelegramCodePrompt` (строки ~4339-4493)
- В `telegram-webhook/index.ts` добавлена логика генерации кода при `/start` (строки 148-214)
- Автопроверка статуса каждые 3 секунды для старого способа работает

---

## 🔧 ЧТО НУЖНО ДОДЕЛАТЬ:

### ЗАДАЧА: Доработать новый способ привязки (из Telegram)

**Сценарий:**
1. Пользователь пишет `/start` боту (без токена)
2. Бот генерирует 6-значный код и отправляет пользователю
3. Пользователь открывает приложение, входит с email/паролем
4. **После входа автоматически показывается экран** `TelegramCodePrompt` с полем для ввода кода
5. Пользователь вводит код
6. Система находит `tg_id` по коду и привязывает к `employee.id`
7. Готово! Кнопка становится синей

---

## ❌ ЧТО СЕЙЧАС НЕ РАБОТАЕТ:

### Проблема: Бот перестал отвечать на `/start`

**Причина:** После редактирования Edge Function через Supabase UI сбросилась настройка "Verify JWT"

**Решение (УЖЕ НАЙДЕНО ПОЛЬЗОВАТЕЛЕМ):**
1. Supabase Dashboard → Edge Functions → telegram-webhook
2. Три точки → Settings (или маленький уголок с настройками)
3. Найти опцию **"Verify JWT"** → **ОТКЛЮЧИТЬ** ✅
4. Сохранить

**После отключения JWT бот снова работает!**

---

## 📋 ЧТО ДЕЛАТЬ ДАЛЬШЕ:

### Шаг 1: Проверить, что бот работает

```bash
# В Telegram напишите боту:
/start
```

**Ожидается:**
- Если уже привязан: "✅ Аккаунт уже привязан!"
- Если НЕ привязан: Сообщение с 6-значным кодом (например: `123456`)

---

### Шаг 2: Обновить Edge Function с новым кодом

**ВАЖНО:** После деплоя обязательно проверить, что "Verify JWT" остаётся **ОТКЛЮЧЕН**!

1. Supabase Dashboard → Edge Functions → telegram-webhook → Edit
2. Скопировать код из `supabase/functions/telegram-webhook/index.ts` (полная версия с обоими способами)
3. Deploy
4. **СРАЗУ ПРОВЕРИТЬ:** Settings → Verify JWT → ОТКЛЮЧИТЬ (если включилось обратно)

---

### Шаг 3: Протестировать новый способ

1. В Telegram → `/start` боту
2. Получить код (6 цифр)
3. Открыть приложение `http://localhost:5173`
4. **Выйти из аккаунта** (если вошли)
5. Войти заново с email/паролем
6. **Должен появиться экран "Привязка Telegram"** с полем для ввода кода
7. Ввести код
8. Нажать "Привязать Telegram"
9. Должно появиться: "✅ Telegram успешно привязан!" и страница перезагрузится
10. Кнопка станет синей: "📱 Telegram подключён"

---

## 🐛 Возможные проблемы:

### Проблема 1: Экран ввода кода не появляется после входа

**Проверить:**
- В `app.js` строка ~4406: `var showCodePromptState = useState(!props.employee.tg_id);`
- Убедиться, что `props.employee.tg_id` действительно `null` в базе
- В функции `fetchEmployeeForUser` (строка ~121) должно быть: `.select("id, first_name, last_name, is_active, tg_id")`

**Отладка:**
- Открыть консоль браузера (F12)
- При входе должен быть лог: `fetchEmployeeForUser result: {id: X, tg_id: null, ...}`
- Если `tg_id` не `null` — экран не появится

---

### Проблема 2: Бот не генерирует код при `/start`

**Проверить:**
- Логи Edge Function: должна быть запись "Generated code: 123456"
- Если нет логов → webhook сломался (проверить `getWebhookInfo`)
- Если есть ошибка в логах → проблема с SQL INSERT

**Решение:**
- Проверить, что SQL выполнен (поле `tg_id` добавлено в таблицу)
- Проверить, что `employee_id` стало nullable
- Посмотреть точную ошибку в логах Supabase

---

### Проблема 3: Код не находится в приложении

**Проверить:**
- В `TelegramCodePrompt` (строка ~4544): 
  ```javascript
  .eq("token", code.trim())
  .eq("used", false)
  .is("employee_id", null)
  .not("tg_id", "is", null)
  ```
- Убедиться, что запись в БД существует с `employee_id = NULL` и `tg_id != NULL`

---

## 📁 КЛЮЧЕВЫЕ ФАЙЛЫ:

```
app.js (строки 4339-4493)          # Компонент TelegramCodePrompt
app.js (строки 4406-4418)          # Логика показа экрана кода
app.js (строки 121)                # fetchEmployeeForUser - должен загружать tg_id

telegram-webhook/index.ts          # Новая версия (оба способа)
telegram-webhook/index-WORKING-OLD.ts  # Старая рабочая версия (только способ 1)

SQL_UPDATE_TELEGRAM_TOKENS.sql     # SQL для обновления таблицы (УЖЕ ВЫПОЛНЕН)
```

---

## 🎯 ЗАДАЧА ДЛЯ СЛЕДУЮЩЕГО АГЕНТА:

**Сделать так, чтобы работал новый способ привязки:**
1. Пользователь пишет `/start` в Telegram
2. Получает 6-значный код
3. Входит в приложение
4. Видит экран ввода кода
5. Вводит код
6. Привязка происходит автоматически

**ВАЖНО:**
- Старый способ (из приложения) должен продолжать работать!
- Оба способа работают параллельно
- При деплое Edge Function **обязательно отключать "Verify JWT"**!

---

## 🔑 КРИТИЧЕСКИ ВАЖНО:

### После ЛЮБОГО изменения Edge Function:
1. Deploy
2. **СРАЗУ** проверить: Settings → Verify JWT → **ДОЛЖНО БЫТЬ ВЫКЛЮЧЕНО**
3. Если включилось — отключить
4. Тест: `/start` боту → должен ответить

### Webhook может отвалиться:
Если бот не отвечает:
```
1. Проверить: api.telegram.org/bot.../getWebhookInfo
2. Если ошибка 401 → отключить Verify JWT
3. Если нет URL → установить заново: api.telegram.org/bot.../setWebhook?url=...
```

---

## 📞 Контакты бота:

- Username: `@money_cheking_bot`
- Token: `8477674658:AAHdZS8bGIKINlXawLoNJiuukywWQgAt3E0`
- Webhook URL: `https://ervtmgbehtwd5cvbnvio.supabase.co/functions/v1/telegram-webhook`

---

## ✅ СТАТУС:

- Блоки 01-06: **ГОТОВЫ** ✅
- Блок 07 (Telegram): **ЧАСТИЧНО ГОТОВ**
  - Способ 1 (из приложения): ✅ Работает
  - Способ 2 (из Telegram): ⏳ Код написан, нужно протестировать и отладить

---

**УДАЧИ СЛЕДУЮЩЕМУ АГЕНТУ!** 🚀

Главное помнить про **Verify JWT** — это ключевая настройка для работы с Telegram!
