# Roadmap — Энергомониторинг

Документ-карта проекта. Меняем его по шагам до достижения нужного состояния. Каждый следующий шаг реализации опирается на этот план.

---

## 1. Роли и права

### 1.1 Роль «Владелец» (Owner / Boss)

- Один главный пользователь (или несколько с полными правами).
- **Может:**
  - Всё: объекты, архив, статистика, создание/редактирование/удаление, перевод в архив, исправление данных.
  - Управление пользователями: создавать учётки, назначать роли и права.
- **Интерфейс:** отдельная кнопка при входе (или отдельный раздел) — «Управление пользователями» / «Создать пользователя».

### 1.2 Роль «Пользователь» (User)

- Обычный пользователь с ограниченным доступом.
- **Может:**
  - Видеть только **назначенные ему объекты** (не все).
  - Вносить показания (заполнять данные по объектам).
  - Смотреть статистику (в рамках своих объектов).
  - Создавать новое (в рамках разрешённого — уточнить: только показания или ещё что-то).
- **Не может:**
  - Редактировать данные объекта (адрес, площадь, контакты и т.д.).
  - Открывать архив.
  - Переводить объекты в архив.
  - Корректировать введённые данные (или только в рамках политики — уточнить).
- **Telegram:** привязка Telegram для роли «Пользователь» **пока недоступна** (сделаем отдельным шагом позже).

### 1.3 Итог по ролям

| Возможность | Владелец (Owner/Boss) | Пользователь (User) |
|-------------|------------------------|----------------------|
| Видеть объекты | Все | Только назначенные |
| Вносить показания | Да | Да |
| Создавать объекты | Да | Нет (или ограниченно — уточнить) |
| Редактировать объект | Да | Нет |
| Архив (просмотр/восстановление) | Да | Нет |
| Перевод в архив | Да | Нет |
| Корректировка данных | Да | Нет |
| Статистика | Вся | Только по своим объектам |
| Управление пользователями | Да | Нет |
| Привязка Telegram | Да (позже) | Пока нет |

---

## 2. Управление пользователями (для Владельца)

- Кнопка в интерфейсе: «Создать пользователя» / «Управление пользователями».
- Владелец указывает:
  - Роль (Пользователь / или другие, если появятся).
  - Что пользователь может делать (в соответствии с ролью).
  - Email.
  - Логин и пароль (или только email — тогда отправка приглашения с ссылкой/паролем).
- После сохранения — пользователю отправляется уведомление (email/сообщение): «Вам создан доступ: логин, пароль» (механизм отправки — отдельный шаг).
- **Telegram-авторизация:** пока не делаем, выносим в отдельный этап. В коде/интерфейсе ничего не программируем под Telegram-вход на этом шаге.

---

## 3. Платформа: браузер и mini-app

- **Основной сценарий:** работа в **обычном браузере** (с телефона или с компьютера). Сайт должен открываться и корректно работать в **полноэкранном режиме**, чтобы было видно много информации.
- **Mini-app в Telegram** — опция: можно открыть приложение и из Telegram, но это не обязательный и не единственный способ. Приоритет — удобство в браузере и полный экран.

---

## 4. Порядок шагов (что делаем по очереди)

**Правило:** каждый шаг сначала делаем, потом **проверяем, что всё работает**. Только после проверки ставим в колонке **«Готово»** галочку ✅. К следующему шагу переходим **только когда текущий отмечен как готовый**.

| # | Шаг | Статус | Готово (проверено) | Примечание |
|---|-----|--------|--------------------|------------|
| 0 | **Создать roadmap (этот документ)** | ✅ Сделано | ✅ | Карта проекта |
| 1 | Ввести роли в БД и в приложении (Owner vs User) | ✅ Сделано | ✅ | Поле `role`, подпись (Владелец)/(Пользователь) в меню. |
| 2 | Привязка объектов к пользователю (доступ только к «своим» объектам) | ✅ Сделано | ✅ | Поле `assigned_employee_id` в objects ([SQL_STEP2_ASSIGNED_EMPLOYEE.sql](SQL_STEP2_ASSIGNED_EMPLOYEE.sql)); в «Создать объект» и «Редактировать объект» — выбор «Закрепить за пользователем»; User видит только объекты, где assigned_employee_id = его id. |
| 3 | Ограничить UI для роли User: нет архива, нет редактирования объекта, нет перевода в архив | ✅ Сделано | ✅ | У User скрыты: кнопка Telegram, пункты меню «Создать объект», «Редактировать объект», «Архив». Видны только «Внести показания» и «Статистика». |
| 4 | Раздел «Управление пользователями» для Владельца: кнопка, форма (роль, права, email, логин, пароль) | ⏳ Ждёт | — | Создание учётки и отправка данных пользователю |
| 5 | Отправка пользователю данных для входа (email/сообщение) | ⏳ Ждёт | — | После шага 4 |
| 6 | Адаптация интерфейса под полный экран в браузере (много информации на экране) | ⏳ Ждёт | — | Верстка, раскладки |
| 7 | Telegram: авторизация и привязка (отдельный этап, позже) | 🔜 Позже | — | Пока не программируем |

### Проверка шага 1 (перед галочкой «Готово»)

1. В Supabase → SQL Editor выполнить скрипт **SQL_STEP1_EMPLOYEE_ROLES.sql**.
2. В Table Editor → **employees** одному сотруднику выставить **role** = `owner` (остальные — `user`).
3. Открыть приложение, войти под сотрудником с ролью **owner** — в главном меню должно быть: **«Сотрудник: Имя Фамилия (Владелец)»**.
4. Войти под сотрудником с ролью **user** — должно быть: **«(Пользователь)»**.
5. Если всё так — в таблице выше у шага 1 в колонке «Готово (проверено)» поставить ✅ и в «Статус» написать ✅ Сделано. После этого переходить к шагу 2.

### Проверка шагов 2 и 3 (после выполнения SQL и входа под разными ролями)

1. Выполнить в Supabase **SQL_STEP2_ASSIGNED_EMPLOYEE.sql** (колонка `assigned_employee_id` в таблице `objects`).
2. Войти под **Владельцем**: в меню видны кнопка Telegram, «Внести показания», «Создать объект», «Редактировать объект», «Статистика», «Архив». В формах «Создать объект» и «Редактировать объект» есть поле **«Закрепить за пользователем»** (список сотрудников с ролью user; можно оставить «— Не назначен —»).
3. Назначить одному или нескольким объектам в «Редактировать объект» конкретного пользователя (роль user).
4. Войти под **Пользователем**: в меню только «Внести показания» и «Статистика» (без Telegram, без Создать/Редактировать объект, без Архива). В «Внести показания» и «Статистика» видны только объекты, у которых в «Закрепить за пользователем» выбран этот пользователь.
5. Если список в «Закрепить за пользователем» пустой — проверить RLS на таблице `employees`: владелец должен иметь право читать строки с `role = 'user'` (например, политика SELECT для аутентифицированных, где `role = 'user'` или `auth_user_id = auth.uid()`).

---

## 5. Связь с другими документами

- **db_ui_mapping.md** — соответствие полей БД и UI; при добавлении ролей и прав его нужно будет обновить.
- **master_system_plan.md** / **implementation_plan.md** — общая архитектура; при расхождении с roadmap правим план под этот документ.

---

## 6. Как пользоваться roadmap

- Меняем этот файл по шагам: вносим правки в роли, права, таблицу шагов.
- **Порядок работы:** делаем один шаг → проверяем в браузере/БД, что всё работает → в колонке «Готово (проверено)» ставим ✅ → только тогда переходим к следующему шагу.
- Когда шаг выполнен и проверен — в «Статус» можно поставить ✅ Сделано, в «Примечание» кратко зафиксировать, что сделано.
